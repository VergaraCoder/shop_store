interface ICategoryProductErrors {
  NOT_FOUND: string;
  NOT_FOUNDS: string;
}

export const CATEGORY_PRODUCT_ERRORS: ICategoryProductErrors = {
  NOT_FOUND: 'Category product not found',
  NOT_FOUNDS: 'THERE ARE NOT CategoryProductS',
};
